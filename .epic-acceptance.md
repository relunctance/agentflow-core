# AgentFlow Epic 验收标准

## Epic-D 验收标准（0-1 MVP）

### 目标
单节点跑通"Agent注册 → 心跳 → 状态上报 → 实时页面展示 → 飞书异常告警"全链路。

### 启动步骤

```bash
# 1. 启动 Go 核心服务（端口 8080）
export PATH=$HOME/.local/go/bin:$PATH
cd /home/gql/.openclaw/workspace/agentflow-core
go run ./cmd/
# 期望：显示 "agentflow-core is running on :8080"

# 2. 启动 PHP 页面（端口 8081）
php -S 0.0.0.0:8081 -t . &
# 期望：显示 "PHP Development Server started on 0.0.0.0:8081"
```

### 验收清单

| # | 功能 | 验收命令 | 期望结果 |
|---|------|---------|---------|
| 1 | Agent注册 | `curl -X POST http://localhost:8080/api/agents -H "Content-Type: application/json" -d '{"name":"test-agent","model":"gpt-4","provider":"openai"}'` | 返回 `{"id":"xxx","name":"test-agent","status":"active"...}` |
| 2 | 心跳上报 | `curl -X POST http://localhost:8080/api/agents/{id}/heartbeat` | 200返回，心跳时间更新 |
| 3 | 状态上报（running） | `curl -X POST http://localhost:8080/api/agents/{id}/status -H "Content-Type: application/json" -d '{"status":"running","description":"任务进行中"}'` | 200返回，状态为running |
| 4 | 状态上报（failed） | `curl -X POST http://localhost:8080/api/agents/{id}/status -H "Content-Type: application/json" -d '{"status":"failed","description":"测试失败"}'` | 飞书收到告警通知 |
| 5 | SSE实时页面 | 浏览器访问 `http://localhost:8081/internal/monitor/monitor.php` | 看到Agent列表，状态实时刷新 |
| 6 | 离线检测 | 停止心跳90秒后查询Agent状态 | Agent状态自动变为 `offline` |
| 7 | Go测试 | `go test ./...` | 全部 PASS |

### 飞书配置（如需测试告警）
1. 在飞书开放平台创建机器人，获取 WebHook 地址
2. 修改 `internal/notify/lark.go` 中的 WebHook URL
3. 重启 Go 服务
